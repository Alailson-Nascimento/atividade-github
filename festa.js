// Definindo a variavel com let
let idade = 20;

// Usando o operador ternário

let resultado = idade >= 18 ? "Pode entrar na festa" : "Não pode entrar";

// Exibindo resultado no console
 console.log(resultado);