name: conversa

on:
  push:
      branches:
              - main

jobs:
    escrever-commit:
        runs-on: ubuntu-latest

        steps:
            - name: Nosso primeiro workflow
              run:  "Nosso primeiro workflow"